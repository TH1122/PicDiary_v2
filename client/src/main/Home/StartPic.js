import styled from "styled-components";

const StartPicContainer = styled.div``;
const StartPic = () => {
  return (
    <>
      <StartPicContainer></StartPicContainer>
    </>
  );
};

export default StartPic;
