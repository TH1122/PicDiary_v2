import styled from "styled-components";

const StartCategoryContainer = styled.div``;
const StartCategory = () => {
  return (
    <>
      <StartCategoryContainer></StartCategoryContainer>
    </>
  );
};

export default StartCategory;
